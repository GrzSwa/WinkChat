# Miscellaneous
*.class
*.log
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IDE — IntelliJ / Android Studio (flutter create je generuje; nie commitujemy)
*.iml
*.ipr
*.iws
.idea/

# IDE — Zed (lokalna konfiguracja edytora per projekt)
.zed/

# Flutter / Dart / Pub
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/

# FVM — commitujemy .fvmrc (przypięta wersja), ignorujemy SDK/symlink
.fvm/

# Obfuskacja / symbolication (build release)
app.*.symbols
app.*.map.json

# Pliki środowiskowe z sekretami
.env*

# UWAGA: pubspec.lock CELOWO nie jest tu wymieniony — to APLIKACJA, nie
# biblioteka, więc lockfile COMMITUJEMY (powtarzalne buildy: Ty + CI na
# identycznych wersjach zależności). Patrz GIT_WORKFLOW.md §5 i TECH_STACK.md §4.
