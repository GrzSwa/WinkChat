import 'package:flutter/material.dart';

class SearchCallerScreen extends StatelessWidget {
  const SearchCallerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Search caller Screen'),
      ),
    );
  }
}
