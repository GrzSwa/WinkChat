import 'package:flutter/material.dart';

class RegulationsAndPrivacyPolicyScreen extends StatelessWidget {
  const RegulationsAndPrivacyPolicyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
